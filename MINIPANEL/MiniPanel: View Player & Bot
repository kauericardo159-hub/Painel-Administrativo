local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer

--[[ 
===========================================================
       ⚙ GERENCIADOR DO MINI-PAINEL V10 (ELITE DESIGN) ⚙
===========================================================
]]

-- 1. LOCALIZAÇÃO NA LISTA
local playerGui = player:WaitForChild("PlayerGui")
local coreGui = playerGui:WaitForChild("SistemaPainel_V3")
local painel = coreGui:WaitForChild("Panel")
local lista = painel:WaitForChild("ListaOpcoesContainer")

-- Limpeza de duplicatas
if lista:FindFirstChild("ViewControlCard") then
    lista.ViewControlCard:Destroy()
end

-- 2. CRIAÇÃO DO MINI-PAINEL (Design Refinado)
local miniPainel = Instance.new("Frame")
miniPainel.Name = "ViewControlCard"
miniPainel.Size = UDim2.new(0.96, 0, 0, 115) 
miniPainel.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
miniPainel.BackgroundTransparency = 0.4 
miniPainel.BorderSizePixel = 0
miniPainel.ZIndex = lista.ZIndex + 10 
miniPainel.Parent = lista

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 14)
corner.Parent = miniPainel

-- Borda com Gradiente sutil
local stroke = Instance.new("UIStroke")
stroke.Thickness = 1.5
stroke.Color = Color3.fromRGB(255, 255, 255)
stroke.Transparency = 0.7
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
stroke.Parent = miniPainel

-- Título Centralizado e Elegante
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.Position = UDim2.new(0, 0, 0, 5)
title.BackgroundTransparency = 1
title.Text = "SYSTEM VIEWERS"
title.TextColor3 = Color3.fromRGB(200, 200, 200)
title.Font = Enum.Font.GothamBold
title.TextSize = 13
title.TextTransparency = 0.3
title.ZIndex = miniPainel.ZIndex + 1
title.Parent = miniPainel

-- 3. FUNÇÃO DE CRIAÇÃO DO TOGGLE (Estilo Switch Pro)
local function CreateToggle(name, labelText, posX, loadstringFunction)
    local container = Instance.new("Frame")
    container.Name = name .. "Container"
    container.Size = UDim2.new(0.45, 0, 0, 60)
    container.Position = UDim2.new(posX, 0, 0.62, 0)
    container.AnchorPoint = Vector2.new(0.5, 0.5)
    container.BackgroundTransparency = 1
    container.ZIndex = miniPainel.ZIndex + 1
    container.Parent = miniPainel

    local iconLabel = Instance.new("TextLabel")
    iconLabel.Size = UDim2.new(1, 0, 0, 20)
    iconLabel.Position = UDim2.new(0, 0, 0, 0)
    iconLabel.Text = labelText
    iconLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    iconLabel.Font = Enum.Font.GothamMedium
    iconLabel.TextSize = 14
    iconLabel.BackgroundTransparency = 1
    iconLabel.ZIndex = miniPainel.ZIndex + 2
    iconLabel.Parent = container

    -- Cores Modernas
    local colorInactiveBase = Color3.fromRGB(40, 15, 15)
    local colorInactiveStroke = Color3.fromRGB(200, 50, 50)
    local colorActiveBase = Color3.fromRGB(15, 40, 25)
    local colorActiveStroke = Color3.fromRGB(50, 200, 100)

    -- Botão (Fundo)
    local btnBase = Instance.new("TextButton")
    btnBase.Size = UDim2.new(0, 50, 0, 24)
    btnBase.Position = UDim2.new(0.5, 0, 0.75, 0)
    btnBase.AnchorPoint = Vector2.new(0.5, 0.5)
    btnBase.BackgroundColor3 = colorInactiveBase
    btnBase.Text = ""
    btnBase.AutoButtonColor = false
    btnBase.ZIndex = miniPainel.ZIndex + 2
    btnBase.Parent = container

    Instance.new("UICorner", btnBase).CornerRadius = UDim.new(1, 0)
    
    local btnStroke = Instance.new("UIStroke")
    btnStroke.Thickness = 1.5
    btnStroke.Color = colorInactiveStroke
    btnStroke.Parent = btnBase

    -- Knob (Círculo)
    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 18, 0, 18)
    knob.Position = UDim2.new(0.1, 0, 0.5, 0)
    knob.AnchorPoint = Vector2.new(0, 0.5)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.ZIndex = btnBase.ZIndex + 1
    knob.Parent = btnBase
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    -- LÓGICA DE CLIQUE (Com Notificação)
    local isActive = false
    btnBase.MouseButton1Click:Connect(function()
        isActive = not isActive
        
        -- Disparar Notificação Estilosa
        if _G.NotifyElite then
            local statusLabel = isActive and "ATIVADO" or "DESATIVADO"
            _G.NotifyElite("VIEWER SYSTEM", labelText .. " foi " .. statusLabel .. "!", isActive)
        end

        local targetPos = isActive and UDim2.new(0.9, -18, 0.5, 0) or UDim2.new(0.1, 0, 0.5, 0)
        local targetBaseColor = isActive and colorActiveBase or colorInactiveBase
        local targetStrokeColor = isActive and colorActiveStroke or colorInactiveStroke

        TweenService:Create(knob, TweenInfo.new(0.3, Enum.EasingStyle.Back), {Position = targetPos}):Play()
        TweenService:Create(btnBase, TweenInfo.new(0.3), {BackgroundColor3 = targetBaseColor}):Play()
        TweenService:Create(btnStroke, TweenInfo.new(0.3), {Color = targetStrokeColor}):Play()

        task.spawn(loadstringFunction)
    end)
end

-- 4. CHAMADA DOS TOGGLES
CreateToggle("PlayerView", "👤 Player", 0.28, function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/kauericardo159-hub/Painel-Administrativo/refs/heads/main/SYSTEM/SYSTEM%3A%20View%20Player"))()
end)

CreateToggle("BotView", "🤖 Bot", 0.72, function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/kauericardo159-hub/Painel-Administrativo/refs/heads/main/SYSTEM/SYSTEM%3A%20View%20Bot"))()
end)

-- 5. ANIMAÇÃO DE ENTRADA
local function animateEntry()
    local originalPos = UDim2.new(0.02, 0, 0, 0)
    miniPainel.Position = UDim2.new(1.5, 0, 0, 0)
    miniPainel.BackgroundTransparency = 1
    stroke.Transparency = 1
    
    task.wait(0.5)
    TweenService:Create(miniPainel, TweenInfo.new(0.8, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
        Position = originalPos,
        BackgroundTransparency = 0.4
    }):Play()
    TweenService:Create(stroke, TweenInfo.new(0.8), {Transparency = 0.7}):Play()
end

animateEntry()

print("✅ MiniPainel V10: Agora com Notificações Elite integradas!")
